name: Apply ContinueWatching v5.4 strict identity

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-continuewatch-v54.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Patch ContinueWatching
        run: python3 scripts/patch_continuewatch_v54.py

      - name: Syntax check
        run: node --check ContinueWatching.js

      - name: Publish cache-safe copy
        run: cp ContinueWatching.js ContinueWatching-v5.4.js

      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ContinueWatching.js ContinueWatching-v5.4.js
          git commit -m "Fix ContinueWatching strict card identity and profile isolation" || exit 0
          git push
